# Bundles

Multi-folder packs that share one set of business files — for owners running more than one thing (a salon plus a booth-rental side, a café plus a market stall). Planned; nothing ships here yet. A bundle will always merge into one morning brief and one end-of-day close, so a pack never doubles the owner's attention.
